# Copy to /etc/X11/Xsession.d/ on the raspi and make sure it has -rw-r--r--.
# Make sure it is one of the last scripts called (e.g. rename 200raspi-radio to
# 250raspi-radio if needed).

sudo -u pi /home/pi/raspi-radio/start.sh --format json &
